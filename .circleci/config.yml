 version: 2.1

 orbs:
  win: circleci/windows@2.2.0

 jobs:
   build:
     executor: win/default     
    
     steps:
       - checkout
       - run: $env:DOTNET_CLI_TELEMETRY_OPTOUT = '1'
       - run: dotnet build
